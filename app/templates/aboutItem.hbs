{{!-- This template populates the copy for each button in the AboutUs template. To add/change the copy, access
the array variable called 'abouts' in the router.js file --}}
{{!-- If you make changes to any of the templates within the app folder, after saving your changes you will need to run 'grunt build' in your terminal. This will rebuild the dist file which is designed for 
use in production. --}}
<h3>{{title}}</h3>
{{{summary}}}